I am the superclass of context-specific classes that need access to the current session and context, and need to be able to respond.

API:

- Use respond: to quit the current request processing and respond.